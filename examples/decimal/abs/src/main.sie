import std.io;
import { Decimal } from std.decimal;

fn main(args: const char*[]) -> i32 {
    if (args.length != 2) {
		std.io.println(std.io.stderr, "abs: usage: {} x", args[0]);
		return 1;
	}

    let dec = Decimal(args[1]);

    std.io.println("{}", dec.abs());

    return 0;
}