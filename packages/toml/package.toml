[package]
name = "toml"
description = "TOML parser"
version = "1.0.0"
license = "BSD-3-Clause"
keywords = ["toml", "parser", "markup"]

[library]
sources = ["src/"]

[dependencies]
core = "*"
decimal = "*"
