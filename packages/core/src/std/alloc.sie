import stdlib;

@include("_alloc/_allocator");
@include("_alloc/_stdlib");
@include("_alloc/_heap");

@inline
fn alloc<T>(n: u64 = 1) -> T* {
    return stdlib.malloc(n * @sizeof(T)) as T*;
}

@inline
fn free(ptr: opaque*) {
    stdlib.free(ptr);
}
