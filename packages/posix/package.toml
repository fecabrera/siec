[package]
name = "posix"
description = "Bindings for C's POSIX library"
version = "1.0.0"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["bindings", "posix", "unix", "unistd"]

[library]
sources = ["src/"]
