[package]
name = "libc"
description = "Bindings for the C Standard Library"
version = "1.0.0"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["bindings", "libc"]

[library]
sources = ["src/"]
