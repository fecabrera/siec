[package]
name = "openssl"
description = "Bindings for OpenSSL"
version = "1.0.0"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["bindings", "openssl", "ssl", "crypto", "encryption", "decryption", "digest"]

[library]
sources = ["src/"]
libs = ["ssl", "crypto"]

[dependencies]
libc = "~1"
