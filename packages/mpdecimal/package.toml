[package]
name = "mpdecimal"
description = "Bindings for mpdecimal"
version = "1.0.0"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["bindings", "mpdecimal", "libmpdec", "decimal"]

[library]
sources = ["src/"]
libs = ["mpdec"]

[dependencies]
libc = "~1"
