[package]
name = "archive"
description = "Library for compressing and decompressing files"
version = "1.0.0"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["archive", "compression", "decompression", "zip", "unzip"]

[library]
sources = ["src/"]

[dependencies]
core = "~1"
libzip = "~1"
