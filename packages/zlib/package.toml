[package]
name = "zlib"
description = "Bindings for zlib"
version = "1.0.0"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["zlib", "libz", "compression"]

[library]
sources = ["src/"]
libs = ["z"]
