[package]
name = "libzip"
description = "A library for reading, creating, and modifying zip archives."
version = "1.11.4"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["libzip", "zip", "archive", "compression", "decompression"]

[library]
sources = ["src/"]
libs = ["zip"]
